export default function App() {
  return (
    <main className="app-shell">
      <section className="panel">
        <p className="eyebrow">AutoStock MVP</p>
        <h1>A-share LLM simulated trading webchat</h1>
        <p>
          The first milestone wires the single-container app shell before the
          chat runtime, providers, and tools are connected.
        </p>
      </section>
    </main>
  );
}
